import {
  LayoutDashboard,
  Database,
  CreditCard,
  FileChartColumn,
  Settings,
} from "lucide-react";

export const NAVIGATION_ICONS = {
  dashboard: <LayoutDashboard size={18} />,

  database: <Database size={18} />,

  "credit-card": <CreditCard size={18} />,

  "file-chart": <FileChartColumn size={18} />,

  settings: <Settings size={18} />,
};
